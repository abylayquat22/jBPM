$kind: http-request
url: "{{baseUrl}}/server/jobs/:jobId/data?containerId=string"
method: POST
headers:
  Content-Type: application/json
queryParams:
  - key: containerId
    value: string
    description: optional container id that the job should be associated with
pathVariables:
  - key: jobId
    value: "123"
    description: identifier of the asynchronous job to be updated
body:
  type: json
  content: string
examples: ./.resources/Updates parameters for job commands, if used..resources/examples
order: 1000
