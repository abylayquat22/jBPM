$kind: http-example
request:
  url: "{{baseUrl}}/server/jobs?containerId=string"
  method: POST
  headers:
    Content-Type: application/json
  queryParams:
    - key: containerId
      value: string
      description: optional container id that the job should be associated with
  body:
    type: json
    content: string
response:
  statusCode: 500
  statusText: Internal Server Error
  body:
    type: text
    content: ""
order: 2000
