$kind: http-request
url: "{{baseUrl}}/server/jobs?containerId=string"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
queryParams:
  - key: containerId
    value: string
    description: optional container id that the job should be associated with
body:
  type: json
  content: string
examples: ./.resources/Schedules a job and returns the ID for the new
  job..resources/examples
order: 7000
