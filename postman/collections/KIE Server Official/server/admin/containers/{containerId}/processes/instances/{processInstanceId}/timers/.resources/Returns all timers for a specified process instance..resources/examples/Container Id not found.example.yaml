$kind: http-example
request:
  url: "{{baseUrl}}:/server/admin/containers/:containerId/processes/instances/:pr\
    ocessInstanceId/timers"
  method: GET
  pathVariables:
    - key: containerId
      value: evaluation_1.0.0-SNAPSHOT
      description: container id that process instance belongs to
    - key: processInstanceId
      value: "123"
      description: identifier of process instance that timer instances should be
        collected for
response:
  statusCode: 404
  statusText: Not Found
  body:
    type: text
    content: ""
order: 2000
