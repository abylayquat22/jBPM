$kind: http-request
url: "{{baseUrl}}/server/queries/processes/instance/correlation/:correlationKey"
method: GET
headers:
  Accept: application/json
pathVariables:
  correlationKey: "22375"
auth:
  type: basic
  credentials:
    password: "{{password}}"
    username: "{{username}}"
examples: ./.resources/Returns information about a single process instance with
  a specified correlation key..resources/examples
order: 1000
