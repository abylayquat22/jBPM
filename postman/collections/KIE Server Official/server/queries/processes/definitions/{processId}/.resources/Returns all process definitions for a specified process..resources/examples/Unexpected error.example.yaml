$kind: http-example
request:
  url: "{{baseUrl}}/server/queries/processes/definitions/:processId"
  method: GET
  pathVariables:
    - key: processId
      value: string
      description: process id to load process definition
response:
  statusCode: 500
  statusText: Internal Server Error
  body:
    type: text
    content: ""
order: 2000
