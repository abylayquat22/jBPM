$kind: http-request
url: "{{baseUrl}}/server/queries/processes/definitions/:processId"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: processId
    value: string
    description: process id to load process definition
examples: ./.resources/Returns all process definitions for a specified
  process..resources/examples
order: 1000
