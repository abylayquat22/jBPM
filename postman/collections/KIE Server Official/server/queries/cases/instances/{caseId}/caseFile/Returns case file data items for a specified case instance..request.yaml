$kind: http-request
url: "{{baseUrl}}/server/queries/cases/instances/:caseId/caseFile?name=string&n\
  ame=string&type=string&type=string&page=0&pageSize=10"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: name
    value: string
    description: optionally filter by data item names
  - key: name
    value: string
    description: optionally filter by data item names
  - key: type
    value: string
    description: optionally filter by data item types
  - key: type
    value: string
    description: optionally filter by data item types
  - key: page
    value: "0"
    description: optional pagination - at which page to start, defaults to 0 (meaning first)
  - key: pageSize
    value: "10"
    description: optional pagination - size of the result, defaults to 10
pathVariables:
  - key: caseId
    value: CASE-0000000001
    description: case instance identifier that data items should belong to
examples: ./.resources/Returns case file data items for a specified case
  instance..resources/examples
order: 1000
