$kind: http-request
url: "{{baseUrl}}/server/queries/containers/:containerId/process/instances"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: status
    value: "3"
    disabled: true
    description: optional process instance status (active, completed, aborted) -
      defaults ot active (1) only
  - key: status
    value: "3"
    disabled: true
    description: optional process instance status (active, completed, aborted) -
      defaults ot active (1) only
  - key: page
    value: "0"
    disabled: true
    description: optional pagination - at which page to start, defaults to 0 (meaning first)
  - key: pageSize
    value: "10"
    disabled: true
    description: optional pagination - size of the result, defaults to 10
  - key: sort
    value: string
    disabled: true
    description: optional sort column, no default
  - key: sortOrder
    value: "true"
    disabled: true
    description: optional sort direction (asc, desc) - defaults to asc
pathVariables:
  - key: containerId
    value: "{{containerId}}"
    description: container id to filter process instance
auth:
  type: basic
  credentials:
    password: "{{password}}"
    username: "{{username}}"
examples: ./.resources/Returns all process instances for a specified KIE
  container..resources/examples
order: 2000
