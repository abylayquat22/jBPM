$kind: http-request
url: "{{baseUrl}}/server/containers/:containerId/cases/instances/:caseId?withDa\
  ta=false&withRoles=false&withMilestones=false&withStages=false"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: withData
    value: "false"
    description: optional flag to load data when loading case instance
  - key: withRoles
    value: "false"
    description: optional flag to load roles when loading case instance
  - key: withMilestones
    value: "false"
    description: optional flag to load milestones when loading case instance
  - key: withStages
    value: "false"
    description: optional flag to load stages when loading case instance
pathVariables:
  - key: containerId
    value: evaluation_1.0.0-SNAPSHOT
    description: container id that case instance belongs to
  - key: caseId
    value: CASE-00000000001
    description: identifier of the case instance
examples: ./.resources/Returns information about a specified case
  instance..resources/examples
order: 10000
