$kind: http-request
url: "{{baseUrl}}/server/containers/:containerId/cases/instances/:caseId/caseFi\
  le/:dataId?restrictedTo=string&restrictedTo=string"
method: POST
headers:
  Content-Type: application/json
queryParams:
  - key: restrictedTo
    value: string
    description: optional role name(s) that given data should be restricted to
  - key: restrictedTo
    value: string
    description: optional role name(s) that given data should be restricted to
pathVariables:
  - key: containerId
    value: evaluation_1.0.0-SNAPSHOT
    description: container id that case instance belongs to
  - key: caseId
    value: CASE-00000000001
    description: identifier of the case instance
  - key: dataId
    value: string
    description: name of the data item to be added to case file
body:
  type: json
  content: string
examples: ./.resources/Adds a single data item as a case file in a specified
  case instance..resources/examples
order: 2000
