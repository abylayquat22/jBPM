$kind: http-example
request:
  url: "{{baseUrl}}/server/containers/:containerId/dmn/openapi"
  method: GET
  pathVariables:
    - key: containerId
      value: string
      description: Container id to retrieve the Model-specific definitions
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
