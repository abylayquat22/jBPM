$kind: http-example
request:
  url: "{{baseUrl}}/server/containers/:containerId/dmn/models/:modelId"
  method: GET
  pathVariables:
    - key: containerId
      value: string
      description: Container id to retrieve the Model-specific definitions
    - key: modelId
      value: string
      description: Reference container-specific Swagger/OAS definitions
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
