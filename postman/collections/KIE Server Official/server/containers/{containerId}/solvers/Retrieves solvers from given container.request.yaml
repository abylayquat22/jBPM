$kind: http-request
url: "{{baseUrl}}/server/containers/:containerId/solvers"
method: GET
headers:
  Accept: application/xml
pathVariables:
  - key: containerId
    value: string
    description: container id where the solvers reside
examples: ./.resources/Retrieves solvers from given container.resources/examples
order: 2000
