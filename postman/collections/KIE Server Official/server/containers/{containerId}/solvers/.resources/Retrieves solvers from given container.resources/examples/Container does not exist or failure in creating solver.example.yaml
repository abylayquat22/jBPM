$kind: http-example
request:
  url: "{{baseUrl}}/server/containers/:containerId/solvers"
  method: GET
  pathVariables:
    - key: containerId
      value: string
      description: container id where the solvers reside
response:
  statusCode: 404
  statusText: Not Found
  body:
    type: text
    content: ""
order: 2000
