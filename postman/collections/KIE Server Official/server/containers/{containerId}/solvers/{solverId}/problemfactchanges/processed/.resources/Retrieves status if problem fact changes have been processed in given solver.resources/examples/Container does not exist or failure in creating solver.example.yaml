$kind: http-example
request:
  url: "{{baseUrl}}/server/containers/:containerId/solvers/:solverId/problemfactc\
    hanges/processed"
  method: GET
  pathVariables:
    - key: containerId
      value: string
      description: container id where the solver resides
    - key: solverId
      value: string
      description: identifier of the solver
response:
  statusCode: 404
  statusText: Not Found
  body:
    type: text
    content: ""
order: 2000
