$kind: http-request
url: "{{base_url}}/kie-server/services/rest/server/healthcheck?report=false"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: report
    value: "false"
    description: optional report flag to return detailed report of the check,
      defaults to false
order: 5000
