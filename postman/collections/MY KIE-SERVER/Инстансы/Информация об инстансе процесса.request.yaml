$kind: http-request
description: 'Generated from cURL: curl -X GET
  "http://reg-jbpm1-lp1:8080/kie-server/services/rest/server/containers/kuat-sample_1.0.0-SNAPSHOT/processes/instances/22298/variables"
  -H "accept: application/json"'
url: "{{base_url}}/kie-server/services/rest/server/containers/{{containerId}}/p\
  rocesses/instances/{{processInstanceId}}"
method: GET
headers:
  accept: application/json
body:
  type: json
  content: ""
auth:
  type: basic
  credentials:
    password: "{{password}}"
    username: "{{username}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Server response status\", function () {\r

      \    if (pm.response.status !== \"OK\") {\r

      \        let msg = pm.response.text();\r

      \        throw new Error(JSON.stringify([msg]));\r

      \    }\r

      \r

      \    let messages = pm.response.json()[\"variable-instance\"].map(i =>
      i[\"value\"]);\r

      \    throw new Error(JSON.stringify(messages))\r

      });"
    language: text/javascript
order: 4000
