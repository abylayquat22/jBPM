$kind: http-request
name: Статическая диаграмма процесса
url: "{{base_url}}/kie-server/services/rest/server/containers/{{containerId}}/i\
  mages/processes/{{processId}}"
method: GET
headers:
  Accept: application/svg+xml
auth:
  type: basic
  credentials:
    password: "{{password}}"
    username: "{{username}}"
order: 1000
