$kind: http-request
description: 'Generated from cURL: curl -X GET
  "http://reg-jbpm1-lp1:8080/kie-server/services/rest/server/admin/cases/instances?page=0&pageSize=10&sortOrder=true&withData=false"
  -H "accept: application/json"'
url: "{{base_url}}/kie-server/services/rest/server/containers/{{containerId}}?%\
  3AabortProcessInstances=true"
method: DELETE
queryParams:
  "%3AabortProcessInstances": "true"
body:
  type: json
  content: "{\r

    \    \"container-id\": \"{{containerId}}\",\r

    \    \"release-id\": {\r

    \        \"group-id\": \"{{groupId}}\",\r

    \        \"artifact-id\": \"{{artifactId}}\",\r

    \        \"version\": \"{{version}}\"\r

    \    }\r

    }"
auth:
  type: basic
  credentials:
    password: "{{password}}"
    username: "{{username}}"
scripts:
  - type: afterResponse
    code: "\r

      pm.test(\"Server response status\", function () {\r

      \    if (pm.response.status !== \"OK\") {\r

      \        let msg;\r

      \        try {\r

      \            msg = pm.response.json().msg;\r

      \        } catch (err) {\r

      \            msg = pm.response.text();\r

      \        } finally {\r

      \            throw new Error(JSON.stringify([msg]));\r

      \        }\r

      \    }\r

      \r

      \    if (pm.response.json().type !== \"SUCCESS\") {\r

      \        throw new Error(JSON.stringify([pm.response.json().msg]));\r

      \    }\r

      });"
    language: text/javascript
order: 3000
